# Secretos y configuración local - NUNCA commitear
.env
.env.local
.env*.local
config.js

# Archivos de sistema
.DS_Store
Thumbbs.db

# Dependencias (usadas solo para optimización local)
node_modules/
package.json
package-lock.json

# Imágenes originales sin optimizar (reemplazadas por .webp)
*.png
*.jpg
*.jpeg
# Excepciones: iconos que deben mantenerse
!icon-192x192.png
